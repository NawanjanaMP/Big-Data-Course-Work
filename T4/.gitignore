.venv/
VIVA_GUIDE.md
README.md

# Neo4j persistent store — regenerated by `docker compose up`, contains
# pre-allocated 256MB transaction log segments that exceed GitHub's 100MB limit
neo4j-data/

# Downloaded raw dataset — regenerate with:
#   curl.exe -L -o cit-Patents.txt.gz https://snap.stanford.edu/data/cit-Patents.txt.gz
cit-Patents.txt.gz

# Derived CSV — regenerated by `python prepare_subset.py cit-Patents.txt.gz`
import/

